{
  "name": "paskahousu",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "node build && chokidar src/ -c 'npm run build' & npm run watchfiles",
    "build": "node build.js",
    "watchfiles": "live-server --wait=800 --quiet"
  },
  "license": "MIT",
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "live-server": "^1.2.1",
    "esbuild": "^0.8.6"
  }
}
